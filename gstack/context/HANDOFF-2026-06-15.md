# Handoff — Bot Financeiro Doméstico (2026-06-15, tarde)

Para o próximo agente continuar o **build** da tarefa pré-produção sem re-alinhar.
Não duplica conteúdo: referencia specs/Vault por caminho. Continua o
`HANDOFF-2026-06-11.md` (infra/itens 1–6) — leia-o para a base do projeto.

## Onde paramos
**Build CONCLUÍDO e commitado localmente; push PENDENTE.** As 2 entregas (seed +
dashboard) foram construídas via TDD (124 testes verdes), rodadas ao vivo na
planilha, revisadas (code-reviewer + workflow-qa APROVADO + security-officer) e
**commitadas em `master` como `d991d21`** (17 arquivos, árvore limpa).

⚠️ **O PUSH NÃO ACONTECEU.** `gh repo create bot-financeiro --private --source=.
--push` foi **negado pelo classificador de segurança** (padrão "Data Exfiltration
bulk-scale" ao criar remote novo + empurrar repo inteiro de PII financeira).
**Nenhum `origin` configurado.** Para destravar, o Marcelo precisa: (a) rodar o
`gh repo create ... --push` no próprio terminal; ou (b) criar o repo vazio e passar
a URL p/ `git remote add origin <url> && git push -u origin master`; ou (c) adicionar
regra de permissão de Bash. **NÃO re-tentar o comando negado automaticamente.**

Estado ao vivo: `Lançamentos` com **443 linhas reais** (seed), abas `Salários`
(Marcelo 20k/Harumi 4k) e `Dashboard` (snapshot maio/2026 + previsão julho/2026)
criadas. Re-rodar `scripts/montar-dashboard.py` antes de cada reunião.

**Próxima ação = destravar o push** (acima). Depois: deploy de produção (servidor
24h clawdinho) + rotação de segredos + planilha a Restrito (backlog abaixo). NÃO há
mais build pendente nesta tarefa.

## A tarefa (3 entregas pré-produção)
1. **Seed** do livro-razão real → aba `Lançamentos`.
2. **Dashboard** (aba Sheets) p/ reunião familiar: mês passado + previsão.
3. **Rateio** proporcional ao salário → número de acerto.

## Fontes de verdade desta tarefa
- Specs aprovadas-com-correções (já corrigidas, prontas p/ build):
  - `gstack/specs/seed-conta-pessoal.md`
  - `gstack/specs/dashboard-reuniao-familiar.md`
  - `gstack/specs/dedup-importacao.md` → **seção "Emenda 15/06"** (dependência do seed)
- Nota de sessão (decisões + raciocínio): `C:\Vault\01_Projetos\bot-financeiro\sessoes\2026-06-15-seed-dashboard-rateio-planejamento.md`
- Dados reais: `Dados CSV/lançamentos conta pessoal entrada.CSV` (35) e `...saida.CSV` (408)
- Esquema da planilha: `scripts/gerar-planilha-inicial.py` (abas `Lançamentos`,
  `Contas Fixas`, `Config`, `Categorias`, `Metas`, `Log`...)
- Padrão de escrita Sheets: `scripts/popular-google-sheet.py` (service account)

## Decisões travadas (NÃO reabrir)
Seed limpa as 76 linhas de teste e semeia limpo • dashboard = aba Sheets • rateio
sobre TODAS as despesas • base = salário fixo (Marcelo 20.000 / Harumi 4.000 →
83,3%/16,7%) • previsão de gastos = contas fixas projetadas + parcelas futuras •
previsão de depósito = `gastos × proporção` • saída = número de acerto • tipo vem
de 2 arquivos separados • coexistência = baseline congelado + dedup cuida •
"mês passado" default = **maio/2026** (parametrizável).

## Pontos críticos que o build NÃO pode esquecer (do plan-reviewer)
1. **Emenda do dedup com `r.status !== "previsto"`** (blacklist). NÃO usar
   `=== "confirmado"` — zera as 19 fixtures de `dedup.test.js:74` (elas não têm `status`).
2. **Projeção de fixas usa a aba `Contas Fixas` (`valor_esperado`, `ativo="sim"`)** —
   Empregada 2240 mensal, Tênis 750 incluído. Não usar "último valor do Lançamentos".
3. **Aba de salário = `Salários`** (nova). NÃO reusar `Config` (esquema `chave|valor`).
4. **Write idempotente**: `values().clear("Lançamentos!A2:J")` + `valueInputOption:"RAW"`
   + datas string `DD/MM/YYYY` (evita coerção a serial).
5. **Parsear CSV por índice de coluna** (header em mojibake `Descri��o`), Latin-1.
6. **Match accent-insensitive** (`Deposito` no CSV vs `Depósito` canônico; fixas idem).
7. **status=previsto** p/ `Data > 15/06/2026`; herança de categoria por `descricao` exata.
8. **Risco guarda-chuva `10/06`**: 1º extrato real pós-seed provavelmente bloqueado
   (retroativo) → reconciliar manualmente; documentar no próximo handoff.

## Ordem de build (TDD) — TODA CONCLUÍDA ✅
1. ✅ Emenda dedup (`filtrarJaImportados` ignora `status="previsto"` no marco) +
   3 testes; workflow `ingestao-csv-conta` regenerado. (E2E ao vivo adiado: no-op
   até existir linha previsto vinda de webhook — o seed grava direto, sem o dedup.)
2. ✅ `workflows/src/seed-parser.js` + 14 testes.
3. ✅ `scripts/seed-conta-pessoal.py` → 443 linhas ao vivo, idempotente.
4. ✅ `workflows/src/rateio.js` + 7 testes.
5. ✅ `workflows/src/dashboard.js` + 4 testes.
6. ✅ `scripts/montar-dashboard.py` (abas + 3 blocos + gráfico) ao vivo.
→ ✅ code-reviewer / ✅ workflow-qa / ✅ security-officer → ✅ commit `d991d21` + Vault.
   Push BLOQUEADO (ver "Onde paramos").

**Snapshot do dashboard é on-demand:** rodar `py -3 scripts/montar-dashboard.py`
(opcional: `... mesPassado mesPrevisao` em MM/YYYY) antes da reunião.
Rodar testes: `node workflows/src/<nome>.test.js`. NUNCA editar JSON de workflow à mão.

## Pendência de segurança específica desta tarefa
`.gitignore` reforçado para `*.xlsx`/`sheets/`/`.CSV`/`.agents/`/`.codex/` (o dump
`sheets/bot-financeiro.xlsx` NÃO deve ser commitado). Salários reais NÃO ficam no
código (`montar-dashboard.py` usa placeholder; valores reais só na aba `Salários`).
Por decisão do Marcelo, nomes/salários/descrições permanecem nos specs/handoff/
testes (repo privado). Lembrar: o `seed-parser.test.js` lê os CSVs reais (em
`Dados CSV/`, gitignored) — a suíte completa só roda numa máquina que tenha esses
arquivos.

## Suggested skills
- `subagent-driven-development` — despachar os subagentes do projeto na ordem
  (plan-reviewer já feito; faltam code-reviewer, workflow-qa, security-officer).
- `verification-before-completion` — antes de marcar cada entrega como ✅
  (rodar testes + E2E, conferir contagens na planilha).
- `handoff` — ao encerrar, atualizar este documento + `log.md` do Vault.

## Pendências de produção/segurança herdadas (backlog, não desta tarefa)
Deploy no servidor 24h (clawdinho) • planilha voltar a Restrito • trocar senha n8n
+ token do bot + authtoken ngrok • apagar xlsx órfão do Drive • limpar `_tmp-diag-lanc`
(FinDiagLanc0001, desativado) no n8n. Segredos: tudo no `.env` (gitignored) — nunca
hardcodar; senha do n8n nunca em arquivo.
